# SKOJ 사용자 E2E 테스트 체크리스트

기준일: 2026-08-25
대상: `skoj.site`의 활성 브라우저 UI와 Django Admin
제외: `/api/` 직접 호출, RSS·Atom·sitemap, 내부 AJAX 단독 계약 테스트, 현재 주석 처리된 Organization·Practice·댓글·투표 기능, 부하·침투 테스트

이 문서는 아직 실행하지 않은 테스트 후보 목록이다. 체크 표시는 실제 E2E 수행 후에만 변경한다.

## 1. 실행 기준

### 1.1 우선순위

- **P0**: 로그인, 문제 풀이·제출·채점, 핵심 권한, 운영 데이터 변경 등 서비스 중단이나 데이터 손실로 이어지는 경로
- **P1**: Guide, 랭킹, 프로필, 대회 운영, 관리자 CRUD 등 주요 업무 경로
- **P2**: 보조 화면, 다운로드, 반응형·접근성·호환성, 예외·경계 조건

### 1.2 사용자 역할

- `GUEST`: 비로그인 사용자
- `INACTIVE`: 가입했지만 이메일 인증이 끝나지 않은 사용자
- `STUDENT-A`: 활성 일반 사용자, A 기수·캠퍼스·반 소속
- `STUDENT-B`: 활성 일반 사용자, 다른 반 소속
- `CONTESTANT`: 진행 중 대회에 참가 중인 사용자
- `AUTHOR`: 자기 문제 편집 권한이 있는 출제자
- `MANAGER`: 문제·대회 운영 권한을 가진 스태프
- `SUPERUSER`: 전체 Django Admin 권한을 가진 최고 관리자
- `BANNED`: 특정 문제 또는 대회에서 차단된 사용자

### 1.3 필수 테스트 데이터

- [ ] `DATA-001 P0` 활성·비활성 기수, 캠퍼스, 반을 각각 준비한다.
- [ ] `DATA-002 P0` GUEST를 제외한 역할별 계정을 준비하고 서로 다른 이메일을 사용한다.
- [ ] `DATA-003 P0` 공개·비공개·대회 전용·암호화·승급전 문제를 준비한다.
- [ ] `DATA-004 P0` 정상 정답, 오답, 컴파일 오류, 런타임 오류, 시간 초과, 메모리 초과를 재현할 소스 코드를 언어별로 준비한다.
- [ ] `DATA-005 P0` 브론즈부터 마스터까지 난이도 클러스터와 단계별 활성 승급전을 준비한다.
- [ ] `DATA-006 P0` 예정·진행·종료·비공개·접근 코드·반 제한·사용자 제한·차단 상태의 대회를 준비한다.
- [ ] `DATA-007 P1` 게시·미게시 Guide, 공개·예약·비공개 해설, 사용자 제출 이력을 준비한다.
- [ ] `DATA-008 P1` 온라인·오프라인 Judge와 활성·비활성 언어를 준비한다.
- [ ] `DATA-009 P1` 메일 수신함, TOTP 앱, WebAuthn 지원 브라우저 또는 가상 인증기를 준비한다.
- [ ] `DATA-010 P2` 데스크톱·태블릿·모바일 viewport와 Chrome·Firefox·Safari 계열 실행 환경을 준비한다.

## 2. 공통 화면과 세션

- [ ] `COMMON-001 P0` GUEST가 홈을 열면 200 응답과 로그인·회원가입 링크를 본다.
- [ ] `COMMON-002 P0` 로그인 사용자가 홈을 열면 사용자 메뉴와 로그아웃을 본다.
- [ ] `COMMON-003 P1` 로그인 상태의 상단 메뉴가 `GUIDE, PROBLEMS, RANKING, USERS, STATUS, ABOUT` 순서로 표시된다.
- [ ] `COMMON-004 P1` GUEST에게 로그인 전용 메뉴가 숨겨지고 공개 메뉴만 정상 동작한다.
- [ ] `COMMON-005 P1` 로고 클릭 시 홈으로 이동한다.
- [ ] `COMMON-006 P1` 현재 페이지에 대응하는 메뉴·탭의 활성 상태가 정확하다.
- [ ] `COMMON-007 P1` 라이트·다크 테마 변경이 즉시 반영되고 새로고침·재로그인 후 유지된다.
- [ ] `COMMON-008 P1` 언어·시간대 변경이 날짜, 상대 시간, 번역 가능한 문구에 반영된다.
- [ ] `COMMON-009 P1` 성공·경고·오류 메시지가 중복되지 않고 사용자가 이해할 수 있게 표시된다.
- [ ] `COMMON-010 P1` 로그아웃 후 보호 페이지를 뒤로가기로 열어도 민감한 내용이 재사용되지 않는다.
- [ ] `COMMON-011 P2` 존재하지 않는 URL은 사용자용 404 화면을 표시하고 내부 traceback을 노출하지 않는다.
- [ ] `COMMON-012 P2` 권한 없는 URL은 403 또는 의도된 404 화면을 표시한다.
- [ ] `COMMON-013 P2` 서버 오류 화면은 일반 사용자에게 traceback·환경 변수·비밀값을 노출하지 않는다.
- [ ] `COMMON-014 P2` 모바일 햄버거 메뉴가 열리고 닫히며 각 링크로 이동한다.
- [ ] `COMMON-015 P2` 여러 탭에서 로그아웃하면 다른 탭의 다음 보호 요청도 로그인 화면으로 이동한다.
- [ ] `COMMON-016 P2` 세션 만료 중 폼을 제출하면 데이터가 조용히 유실되지 않고 재로그인 경로가 명확하다.

## 3. 회원가입·인증·로그인

### 3.1 회원가입과 이메일 인증

- [ ] `AUTH-001 P0` 유효한 아이디·이름·이메일·비밀번호·언어·기수·캠퍼스·반으로 가입한다.
- [ ] `AUTH-002 P0` 가입 직후 INACTIVE 계정과 Profile이 생성되고 선택한 반이 정확히 저장된다.
- [ ] `AUTH-003 P0` 인증 메일 링크로 유효 기간 안에 계정을 활성화하고 로그인한다.
- [ ] `AUTH-004 P0` 이미 사용 중인 아이디를 대소문자·공백 변형별로 차단한다.
- [ ] `AUTH-005 P0` 이미 사용 중인 이메일을 대소문자 변형까지 차단한다.
- [ ] `AUTH-006 P1` 아이디에서 영문자·숫자·밑줄 외 문자를 차단한다.
- [ ] `AUTH-007 P1` 빈 필드, 잘못된 이메일, 약한 비밀번호, 불일치 비밀번호별 오류를 정확한 필드에 표시한다.
- [ ] `AUTH-008 P1` 금지된 이메일 제공자 도메인을 차단한다.
- [ ] `AUTH-009 P1` 활성 기수·캠퍼스를 선택하면 일치하는 활성 반만 선택할 수 있다.
- [ ] `AUTH-010 P1` 기수·캠퍼스와 불일치하는 반 조합을 조작 제출해도 서버가 거부한다.
- [ ] `AUTH-011 P1` 비활성 기수·캠퍼스·반은 신규 가입 선택지에 나타나지 않는다.
- [ ] `AUTH-012 P1` 만료·변조·이미 사용한 활성화 링크가 안전한 오류 화면을 표시한다.
- [ ] `AUTH-013 P1` 미인증 계정으로 로그인하면 인증 필요 안내와 재전송 경로를 제공한다.
- [ ] `AUTH-014 P1` 미인증 계정이 올바른 자격 증명으로 활성화 메일을 재전송한다.
- [ ] `AUTH-015 P1` 활성 계정·틀린 비밀번호·없는 계정의 재전송 요청을 적절히 거부한다.
- [ ] `AUTH-016 P1` 미인증 계정이 올바른 비밀번호로 미사용 이메일로 변경한 뒤 새 메일로 활성화한다.
- [ ] `AUTH-017 P1` 활성 계정, 중복 이메일, 틀린 자격 증명의 이메일 변경을 거부한다.
- [ ] `AUTH-018 P2` 가입 버튼 연속 클릭·새로고침으로 계정이나 메일이 중복 생성되지 않는다.
- [ ] `AUTH-019 P2` 회원가입이 닫힌 설정에서는 가입 폼 대신 가입 불가 화면을 표시한다.

### 3.2 로그인·로그아웃·잠금

- [ ] `AUTH-020 P0` 활성 일반 사용자가 올바른 아이디·비밀번호로 로그인한다.
- [ ] `AUTH-021 P0` `next`가 있는 보호 URL에서 로그인하면 원래 내부 URL로 돌아간다.
- [ ] `AUTH-022 P0` 잘못된 아이디 또는 비밀번호는 동일한 일반 오류를 표시한다.
- [ ] `AUTH-023 P0` 5회 이상 실패 시 10분 로그인 잠금이 적용되고 올바른 비밀번호도 잠금 중 거부된다.
- [ ] `AUTH-024 P1` 잠금 시간이 지난 뒤 정상 로그인할 수 있다.
- [ ] `AUTH-025 P1` 성공 로그인 후 이전 실패 횟수가 초기화된다.
- [ ] `AUTH-026 P1` 로그아웃 POST 후 세션이 종료되고 홈으로 이동한다.
- [ ] `AUTH-027 P1` 로그아웃 URL에 GET 등 잘못된 방식으로 접근해도 CSRF 보호가 우회되지 않는다.
- [ ] `AUTH-028 P1` 관리자 로그인은 비스태프를 거부하고 스태프만 Admin으로 이동시킨다.
- [ ] `AUTH-029 P2` 외부 도메인을 넣은 `next` 값으로 오픈 리다이렉트가 발생하지 않는다.
- [ ] `AUTH-030 P2` 소셜 로그인 설정이 없으면 버튼이 숨겨지고 설정된 공급자만 표시된다.
- [ ] `AUTH-031 P2` 소셜 로그인 성공·거부·공급자 오류가 각각 정상 복귀 또는 안전한 오류 화면으로 이어진다.

### 3.3 계정 찾기·비밀번호

- [ ] `AUTH-032 P0` 등록 이메일로 아이디 찾기를 완료하고 안내 결과를 확인한다.
- [ ] `AUTH-033 P1` 미등록·잘못된 이메일의 아이디 찾기가 계정 존재 여부를 과도하게 노출하지 않는다.
- [ ] `AUTH-034 P0` 일치하는 아이디와 이메일로 비밀번호 재설정 메일을 받는다.
- [ ] `AUTH-035 P0` 유효한 재설정 링크에서 새 비밀번호를 설정하고 새 비밀번호로 로그인한다.
- [ ] `AUTH-036 P1` 아이디·이메일 불일치, 만료·변조·재사용 토큰을 거부한다.
- [ ] `AUTH-037 P1` 로그인 사용자가 기존 비밀번호 확인 후 비밀번호를 변경한다.
- [ ] `AUTH-038 P1` 비밀번호 변경 후 기존 비밀번호는 실패하고 새 비밀번호는 성공한다.
- [ ] `AUTH-039 P2` 비밀번호 재설정 메일 연속 요청이 중복·남용 방지 정책을 따른다.

### 3.4 2단계 인증

- [ ] `AUTH-040 P0` 사용자가 QR/비밀키와 유효한 TOTP 코드로 2FA를 활성화한다.
- [ ] `AUTH-041 P0` 2FA 사용자가 비밀번호 후 유효한 TOTP로 로그인한다.
- [ ] `AUTH-042 P0` 잘못되거나 만료된 TOTP를 거부한다.
- [ ] `AUTH-043 P1` 스크래치 코드를 생성하고 한 번만 로그인에 사용할 수 있다.
- [ ] `AUTH-044 P1` 사용한 스크래치 코드 재사용과 잘못된 형식을 거부한다.
- [ ] `AUTH-045 P1` 유효한 TOTP 확인 후 2FA를 비활성화한다.
- [ ] `AUTH-046 P1` WebAuthn 자격 증명을 등록하고 지원 브라우저에서 로그인한다.
- [ ] `AUTH-047 P1` 변조된 challenge, 다른 origin, 알 수 없는 credential을 거부한다.
- [ ] `AUTH-048 P1` 등록한 WebAuthn 자격 증명을 삭제하고 이후 인증에 사용할 수 없다.
- [ ] `AUTH-049 P1` 2FA 필수 스태프가 설정하지 않은 상태로 Admin에 접근하면 설정 경로로 유도된다.
- [ ] `AUTH-050 P2` 인증 중 취소·새로고침·뒤로가기 후 세션이 인증 완료로 오인되지 않는다.

## 4. 홈·Guide·정보 화면

### 4.1 홈과 게임화 요약

- [ ] `HOME-001 P0` 로그인 사용자의 현재 티어, 가중 점수, 티어별 풀이 수가 저장 데이터와 일치한다.
- [ ] `HOME-002 P0` 승급 조건 미달 시 승급전 카드가 잠김 상태와 남은 조건을 정확히 표시한다.
- [ ] `HOME-003 P0` 조건 충족 시 활성 승급전이 한 번만 해금되고 지정 문제 목록이 표시된다.
- [ ] `HOME-004 P0` 승급전 문제별 미완료·완료 상태와 문제 링크가 정확하다.
- [ ] `HOME-005 P1` 활성 승급전이 없는 티어에서 잘못된 빈 카드나 오류가 나타나지 않는다.
- [ ] `HOME-006 P1` 마스터 사용자는 다음 티어·승급전 안내를 표시하지 않는다.
- [ ] `HOME-007 P1` 랭킹 링크가 전체 랭킹으로 이동한다.
- [ ] `HOME-008 P2` 게임화 카드가 데스크톱 최대 폭과 모바일 레이아웃에서 겹치지 않는다.

### 4.2 Guide

- [ ] `GUIDE-001 P0` GUEST가 `/guides/`와 하위 Guide URL에 접근하면 `next`가 포함된 로그인 화면으로 이동한다.
- [ ] `GUIDE-002 P1` 게시 Guide가 있는 문제 유형만 학습 순서에 표시된다.
- [ ] `GUIDE-003 P1` 유형 카드의 표시명·순서·Guide 수가 DB와 일치한다.
- [ ] `GUIDE-004 P1` 유형을 선택하면 해당 유형의 게시 Guide만 순서대로 표시된다.
- [ ] `GUIDE-005 P1` Guide 상세에서 제목·요약·Markdown 본문이 정확히 렌더링된다.
- [ ] `GUIDE-006 P1` 코드 블록 문법 강조, 복사, 긴 줄 스크롤이 정상 동작한다.
- [ ] `GUIDE-007 P1` 인라인·블록 수식이 로컬 MathJax로 렌더링된다.
- [ ] `GUIDE-008 P1` 미게시 Guide는 일반 사용자에게 노출되지 않고 직접 URL도 거부된다.
- [ ] `GUIDE-009 P1` 스태프에게만 Guide 관리자 편집 버튼이 표시되고 올바른 항목으로 이동한다.
- [ ] `GUIDE-010 P2` 존재하지 않는 유형·Guide ID는 404를 표시한다.
- [ ] `GUIDE-011 P2` 과거 한글 URL이 지원 대상이면 영문 URL로 301 이동한다.
- [ ] `GUIDE-012 P2` Markdown의 위험한 HTML·스크립트가 실행되지 않는다.

### 4.3 공개 정보·상태

- [ ] `INFO-001 P1` About에 SKOJ/SKALA 설명, 개발팀, `skojteam@gmail.com`이 표시된다.
- [ ] `INFO-002 P1` About에서 제거하기로 한 Docs·패치 노트·Special Thanks·과거 기관 문구가 노출되지 않는다.
- [ ] `INFO-003 P1` 이용약관 1~6 탭이 각 URL과 내용으로 이동한다.
- [ ] `INFO-004 P1` Judge 상태 화면에 온라인·오프라인 상태와 최근 ping이 정확히 표시된다.
- [ ] `INFO-005 P1` 언어 목록과 버전 행렬에 활성 런타임 정보가 표시된다.
- [ ] `INFO-006 P2` 언어 통계 화면과 데이터 전환이 빈 데이터·정상 데이터에서 모두 렌더링된다.
- [ ] `INFO-007 P2` 라이선스 상세의 링크·아이콘·본문이 렌더링되고 없는 key는 404다.
- [ ] `INFO-008 P2` favicon·manifest·주요 정적 자산이 HTTPS에서 200이고 혼합 콘텐츠가 없다.

## 5. 문제 탐색·상세·제출

### 5.1 문제 목록과 필터

- [ ] `PROBLEM-001 P0` GUEST가 `/problems/`와 그룹 URL에 접근하면 로그인 화면으로 이동한다.
- [ ] `PROBLEM-002 P0` 일반 사용자는 공개·비대회·비승급전 문제만 본다.
- [ ] `PROBLEM-003 P0` AUTHOR·MANAGER는 권한 범위 안의 비공개·자기 문제를 추가로 본다.
- [ ] `PROBLEM-004 P0` 승급전 문제는 해금 전 일반 목록과 직접 접근에서 숨겨진다.
- [ ] `PROBLEM-005 P0` 해금된 승급전 문제는 해당 사용자만 접근하고 제출할 수 있다.
- [ ] `PROBLEM-006 P1` 루트 문제 목록이 접근 가능한 모든 문제를 표시한다.
- [ ] `PROBLEM-007 P1` 그룹 카드는 접근 가능한 문제가 있는 그룹만 표시하고 문제 수가 정확하다.
- [ ] `PROBLEM-008 P1` `전체 문제`와 선택 그룹의 활성 상태가 정확하다.
- [ ] `PROBLEM-009 P1` 그룹 slug URL은 해당 그룹 문제만 표시하고 한글 표시명을 사용한다.
- [ ] `PROBLEM-010 P1` `bruteforce` 구 URL이 `brute-force`로 301 이동한다.
- [ ] `PROBLEM-011 P1` 레거시 `?category=<id>`가 검색 조건을 보존해 그룹 slug URL로 이동한다.
- [ ] `PROBLEM-012 P1` 잘못된 `groupId` 쿼리가 경로 그룹을 덮어쓰지 않는다.
- [ ] `PROBLEM-013 P1` code·이름 검색의 일치·불일치·한글·공백·특수문자 입력을 처리한다.
- [ ] `PROBLEM-014 P1` 풀이 완료 숨기기와 공개 해설 필터가 켜짐·꺼짐·세션 유지 조건에서 동작한다.
- [ ] `PROBLEM-015 P1` 점수 범위의 최소·최대·동일값·범위 밖 입력을 처리한다.
- [ ] `PROBLEM-016 P1` ID, 문제명, 그룹, 출제자 및 제공되는 모든 정렬을 오름·내림차순으로 전환한다.
- [ ] `PROBLEM-017 P0` 각 정렬에서 20개 페이지 경계의 중복·누락이 없고 새로고침해도 순서가 안정적이다.
- [ ] `PROBLEM-018 P1` 빈 그룹·검색 결과 없음·필터 결과 없음 화면이 깨지지 않는다.
- [ ] `PROBLEM-019 P2` 잘못된 page, 지나치게 큰 page, 음수·문자 page를 안전하게 처리한다.
- [ ] `PROBLEM-020 P2` 랜덤 문제는 현재 접근 가능한 문제로만 이동하며 후보가 없을 때 오류를 안전하게 표시한다.
- [ ] `PROBLEM-021 P2` 문제 표의 ID·문제·그룹·출제자 열이 데스크톱에서 겹치지 않는다.
- [ ] `PROBLEM-022 P2` 모바일에서 그룹 카드, 검색, 표 가로 스크롤이 조작 가능하다.

### 5.2 문제 상세와 해설

- [ ] `PROBLEM-023 P0` 공개 문제 상세에 제목·본문·입출력·제한·허용 언어가 정확히 표시된다.
- [ ] `PROBLEM-024 P0` 비공개·대회 전용·권한 밖 문제는 직접 URL로도 노출되지 않는다.
- [ ] `PROBLEM-025 P0` BANNED 사용자는 차단 문제를 열거나 제출할 수 없다.
- [ ] `PROBLEM-026 P0` 암호화 문제는 틀린·빈 암호를 거부하고 올바른 암호 후 접근을 유지한다.
- [ ] `PROBLEM-027 P1` Markdown, 코드, 표, 이미지, 수식이 본문에서 정상 렌더링된다.
- [ ] `PROBLEM-028 P1` 공개 예정 전 해설은 숨겨지고 공개 시각 이후 표시된다.
- [ ] `PROBLEM-029 P1` 비공개 해설은 권한 없는 사용자에게 숨겨진다.
- [ ] `PROBLEM-030 P1` PDF와 언어별 PDF가 허용 사용자에게 열리고 파일명·Content-Type이 적절하다.
- [ ] `PROBLEM-031 P1` 존재하지 않는 문제 코드는 사용자용 404 메시지를 표시한다.
- [ ] `PROBLEM-032 P2` 긴 문제명·긴 코드 블록·큰 표·이미지가 모바일 레이아웃을 파괴하지 않는다.

### 5.3 코드 제출과 채점

- [ ] `SUBMIT-001 P0` 로그인 사용자가 온라인 Judge가 지원하는 각 기본 언어로 소스를 제출한다.
- [ ] `SUBMIT-002 P0` 빈 소스, 최대 길이 초과, 허용되지 않은 언어, 비활성 언어를 거부한다.
- [ ] `SUBMIT-003 P0` 문제별 허용 언어 제한이 제출 폼과 서버 양쪽에서 적용된다.
- [ ] `SUBMIT-004 P0` 제출 후 상태 페이지로 이동하고 대기·채점 중·완료 상태가 자동 갱신된다.
- [ ] `SUBMIT-005 P0` AC, WA, CE, RE, TLE, MLE, OLE, IE 결과와 점수·시간·메모리를 정확히 표시한다.
- [ ] `SUBMIT-006 P0` 부분 점수 문제에서 testcase별 점수와 총점이 정확하다.
- [ ] `SUBMIT-007 P0` AC 후 프로필 풀이 목록, 홈 카운트, 랭킹 점수가 한 번만 갱신된다.
- [ ] `SUBMIT-008 P0` 같은 문제 재제출·중복 AC가 풀이 수와 가중 점수를 중복 증가시키지 않는다.
- [ ] `SUBMIT-009 P0` 승급전 문제 AC가 해당 시도에 반영되고 모든 문제 AC 시 한 번만 승급한다.
- [ ] `SUBMIT-010 P0` 제출 제한 간격을 위반하면 명확한 오류를 표시하고 중복 제출을 만들지 않는다.
- [ ] `SUBMIT-011 P1` 브라우저 새로고침·뒤로가기·버튼 연속 클릭에도 의도치 않은 중복 제출이 없다.
- [ ] `SUBMIT-012 P1` 기존 자기 제출을 허용된 조건에서 재제출한다.
- [ ] `SUBMIT-013 P1` 다른 사용자의 제출을 권한 없이 재제출할 수 없다.
- [ ] `SUBMIT-014 P1` 제출 상세에서 소스 공개 정책에 따라 소스·testcase 정보가 보이거나 숨겨진다.
- [ ] `SUBMIT-015 P1` 자기 소스 원문·복사 기능이 정확히 동작한다.
- [ ] `SUBMIT-016 P1` 문제별 제출 목록의 사용자·결과·언어·시간 필터와 페이지 이동이 동작한다.
- [ ] `SUBMIT-017 P1` 사용자별 제출 목록은 권한과 공개 정책을 지킨다.
- [ ] `SUBMIT-018 P1` 진행 중 자기 제출은 허용된 상태에서 중단할 수 있고 완료된 제출은 중단할 수 없다.
- [ ] `SUBMIT-019 P1` MANAGER의 재채점이 작업 상태 화면을 거쳐 새 결과로 반영된다.
- [ ] `SUBMIT-020 P1` 권한 없는 사용자는 재채점·대량 재채점·재점수 URL을 실행할 수 없다.
- [ ] `SUBMIT-021 P2` Judge 오프라인·연결 끊김·장시간 대기 상태에서 사용자 안내가 명확하다.
- [ ] `SUBMIT-022 P2` 내부 오류가 발생해도 소스·서버 경로·비밀값이 일반 사용자에게 노출되지 않는다.

## 6. 랭킹·프로필·사용자

### 6.1 게임화와 랭킹

- [ ] `GAME-001 P0` 랭킹은 마스터·다이아몬드·골드·실버·브론즈 순으로 정렬된다.
- [ ] `GAME-002 P0` 같은 티어는 가중 점수와 티어별 풀이 수, 최종 tie-break 기준에 따라 안정적으로 정렬된다.
- [ ] `GAME-003 P0` 비활성·비공개 목록·스태프·슈퍼유저 계정은 랭킹에서 제외된다.
- [ ] `GAME-004 P1` 순위 번호, 사용자 링크, 기수·캠퍼스·반, 현재 티어, 점수가 정확하다.
- [ ] `GAME-005 P1` 난이도 클러스터가 비활성화되거나 가중치가 바뀐 뒤 재계산 결과가 화면에 반영된다.
- [ ] `GAME-006 P0` 각 단계의 요구 풀이 수를 충족하기 전에는 승급전이 해금되지 않는다.
- [ ] `GAME-007 P0` 브론즈→실버→골드→다이아몬드→마스터 승급이 순서대로만 진행된다.
- [ ] `GAME-008 P0` 승급전 해금 뒤 관리자가 문제 구성을 바꿔도 기존 시도는 스냅샷 문제를 유지한다.
- [ ] `GAME-009 P0` 승급전 완료 이력과 완료 시각이 유지되고 재승급이 발생하지 않는다.
- [ ] `GAME-010 P1` 활성 승급전이 중복될 수 없으며 잘못된 관리자 설정이 저장되지 않는다.

### 6.2 사용자 목록과 프로필

- [ ] `USER-001 P1` 사용자 목록이 활성 일반 사용자를 표시하고 기본 정렬·페이지 이동이 안정적이다.
- [ ] `USER-002 P1` 사용자명·이름 검색과 제공되는 정렬의 오름·내림차순이 동작한다.
- [ ] `USER-003 P1` 슈퍼유저·스태프·일반 사용자 표시 정책이 화면 요구사항과 일치한다.
- [ ] `USER-004 P1` 사용자 프로필에 이름, 소속 반, 티어, 풀이·제출 요약이 정확하다.
- [ ] `USER-005 P1` 자기 프로필과 다른 사용자 프로필의 편집·관리 링크 노출이 다르다.
- [ ] `USER-006 P1` 풀이 문제 탭과 사용자 대회 탭이 실제 이력과 일치한다.
- [ ] `USER-007 P1` 사용자 제출 탭의 필터·페이지 이동과 소스 공개 권한이 정확하다.
- [ ] `USER-008 P1` 존재하지 않는 사용자명은 404를 표시한다.
- [ ] `USER-009 P1` 프로필에서 자기소개, 시간대, 언어, 테마를 수정하고 유지한다.
- [ ] `USER-010 P1` 자기소개 1000자 초과를 거부하고 허용된 Markdown을 렌더링한다.
- [ ] `USER-011 P0` 자기소개에 script, event handler, 위험한 style을 넣어도 실행·보존되지 않는다.
- [ ] `USER-012 P1` 실험 기능 권한이 없는 사용자는 해당 옵션을 변경할 수 없다.
- [ ] `USER-013 P2` 사용자 데이터 준비에서 제출·댓글 선택과 문제 glob·결과 필터를 검증한다.
- [ ] `USER-014 P2` 준비 완료 후 자기 데이터만 다운로드할 수 있고 다른 사용자의 파일 URL은 거부된다.
- [ ] `USER-015 P2` 준비 중·실패·만료된 다운로드 작업 상태를 명확히 표시한다.
- [ ] `USER-016 P2` API 토큰 생성 시 새 토큰을 한 번만 노출하고 제거 후 사용할 수 없게 한다.

## 7. 대회

### 7.1 목록·접근·참가

- [ ] `CONTEST-001 P0` GUEST가 대회 목록에 접근하면 로그인 화면으로 이동한다.
- [ ] `CONTEST-002 P1` 예정·진행·종료 대회가 해당 탭과 시간 순서에 맞게 표시된다.
- [ ] `CONTEST-003 P1` 상단 기본 메뉴에는 Contests가 없지만 직접 URL의 활성 대회 기능은 동작한다.
- [ ] `CONTEST-004 P0` 비공개 대회는 권한 없는 사용자 목록·직접 URL에서 노출되지 않는다.
- [ ] `CONTEST-005 P0` 허용 반이 지정된 대회는 동일 반 사용자만 보거나 참가할 수 있다.
- [ ] `CONTEST-006 P0` 허용 사용자 지정 대회는 목록과 직접 참가 모두 사용자 제한을 적용한다.
- [ ] `CONTEST-007 P0` BANNED 사용자는 대회를 보거나 참가·제출할 수 없다.
- [ ] `CONTEST-008 P0` 접근 코드 대회는 빈 값·오답을 거부하고 정답일 때만 참가시킨다.
- [ ] `CONTEST-009 P1` 대회 시작 전에는 문제·랭킹·제출이 설정된 공개 시점까지 숨겨진다.
- [ ] `CONTEST-010 P0` 진행 중 대회 참가 시 ContestParticipation이 한 번만 생성되고 대회 모드 배너가 표시된다.
- [ ] `CONTEST-011 P1` 참가 버튼 연속 클릭·새로고침으로 중복 참가가 생성되지 않는다.
- [ ] `CONTEST-012 P1` 허용된 조건에서 대회를 나가고 현재 대회 상태가 해제된다.
- [ ] `CONTEST-013 P1` 제출 이력이 있거나 정책상 이탈 불가한 상태에서는 나가기를 거부한다.
- [ ] `CONTEST-014 P1` 실제 시작, 가상 참가, 시간 제한, 종료 시각별 남은 시간이 정확하다.
- [ ] `CONTEST-015 P1` 대회 중 다른 보호 페이지에서도 현재 대회 링크·남은 시간이 일관된다.

### 7.2 대회 문제·제출·랭킹

- [ ] `CONTEST-016 P0` 참가자는 대회에 지정된 문제만 순서·라벨·점수와 함께 본다.
- [ ] `CONTEST-017 P0` 비참가자와 권한 없는 사용자는 대회 문제 본문·제출 URL에 직접 접근할 수 없다.
- [ ] `CONTEST-018 P0` 대회 제출이 해당 participation에 연결되고 일반 제출과 혼동되지 않는다.
- [ ] `CONTEST-019 P0` 대회 종료·개인 종료 시각 이후 제출을 거부한다.
- [ ] `CONTEST-020 P1` 늦은 제출 기간이 설정된 경우 허용 시간과 점수 정책을 적용한다.
- [ ] `CONTEST-021 P0` 대회 랭킹의 점수·패널티·문제별 결과·순위가 제출 이력과 일치한다.
- [ ] `CONTEST-022 P1` scoreboard 공개 전·진행 중·종료 후·숨김 설정별 노출 정책이 적용된다.
- [ ] `CONTEST-023 P1` 대회 랭킹 AJAX 새로고침과 전체 페이지 값이 일치한다.
- [ ] `CONTEST-024 P1` 대회 통계의 참가자·언어·결과 집계가 실제 제출과 일치한다.
- [ ] `CONTEST-025 P1` 대회 전체 제출 및 사용자·문제별 제출 목록의 필터와 권한이 동작한다.
- [ ] `CONTEST-026 P1` 자기 participation 상세와 권한 있는 운영자의 타 사용자 participation 상세가 정확하다.
- [ ] `CONTEST-027 P1` 실격 처리 후 해당 사용자의 랭킹·참가 상태가 정책대로 반영된다.
- [ ] `CONTEST-028 P2` 종료 대회 결과 JSON, Excel, 코드 다운로드는 권한·파일명·내용·인코딩을 지킨다.
- [ ] `CONTEST-029 P2` 대회 캘린더, 태그, iCalendar 다운로드가 날짜·시간대를 정확히 표현한다.

### 7.3 대회 운영 도구

- [ ] `CONTEST-030 P1` 권한 있는 운영자가 대회를 복제하고 새 이름·문제 구성이 원본과 독립적이다.
- [ ] `CONTEST-031 P1` 권한 없는 사용자는 clone URL을 사용할 수 없다.
- [ ] `CONTEST-032 P1` JPlag/MOSS 권한 사용자가 분석을 시작하고 진행·성공·실패 상태를 확인한다.
- [ ] `CONTEST-033 P1` 분석 결과 링크와 유사도 자료가 해당 대회 권한자에게만 보인다.
- [ ] `CONTEST-034 P1` JPlag 결과 삭제는 확인 절차 후 대상 대회 결과만 제거한다.

## 8. Django Admin 로그인과 공통 권한

- [ ] `ADMIN-001 P0` GUEST와 일반 사용자는 `/admin/`에 진입할 수 없다.
- [ ] `ADMIN-002 P0` 스태프는 부여된 모델 권한만 메뉴·목록·상세에서 접근한다.
- [ ] `ADMIN-003 P0` URL 직접 입력과 조작 POST로 모델 권한을 우회할 수 없다.
- [ ] `ADMIN-004 P0` SUPERUSER가 전체 등록 모델을 조회·추가·변경·삭제할 수 있다.
- [ ] `ADMIN-005 P1` Admin 검색·필터·정렬·페이지 이동 후 대상 객체가 일관된다.
- [ ] `ADMIN-006 P1` 저장, 저장 후 계속 편집, 복제 저장, 삭제 확인 흐름이 정상 동작한다.
- [ ] `ADMIN-007 P1` 필수값·중복값·잘못된 형식·참조 무결성 오류가 필드 수준으로 표시된다.
- [ ] `ADMIN-008 P1` 변경 이력에 행위자·대상·시각·변경 내용이 기록된다.
- [ ] `ADMIN-009 P1` 읽기 전용 필드는 폼 조작 POST로도 변경할 수 없다.
- [ ] `ADMIN-010 P2` 대량 작업은 선택한 객체에만 적용되고 0개 선택·전체 선택을 안전하게 처리한다.
- [ ] `ADMIN-011 P2` Admin 세션 만료·CSRF 실패·동시 수정 상황에서 잘못된 저장이 발생하지 않는다.
- [ ] `ADMIN-012 P2` 스태프 impersonation 시작·종료 후 권한과 사용자 표시가 즉시 원복된다.

## 9. Django Admin 데이터 관리

### 9.1 사용자·SKALA 조직

- [ ] `AUSER-001 P0` 사용자 추가 시 User와 Profile이 정확히 하나씩 생성된다.
- [ ] `AUSER-002 P0` 사용자 활성·비활성, staff, superuser, 그룹·권한 변경이 로그인과 Admin 접근에 반영된다.
- [ ] `AUSER-003 P0` 사용자 삭제 시 제출·게임화 신호 처리로 외래키 오류가 발생하지 않는다.
- [ ] `AUSER-004 P0` 사용자 삭제가 다른 사용자, Problem, AlgorithmGuide를 삭제하지 않는다.
- [ ] `AUSER-005 P1` 사용자·Profile 검색과 활성·스태프·슈퍼유저·그룹 필터가 정확하다.
- [ ] `AUSER-006 P1` Profile의 반, 공개 목록 여부, 표시 순위, 시간대, 언어를 변경한다.
- [ ] `AUSER-007 P1` 기수는 정수 번호 유일성·활성 상태를 검증한다.
- [ ] `AUSER-008 P1` 캠퍼스 code·name 유일성·활성 상태를 검증한다.
- [ ] `AUSER-009 P1` 반은 `(기수, 캠퍼스, 번호)` 조합 유일성과 활성 상태를 검증한다.
- [ ] `AUSER-010 P1` 참조 중인 기수·캠퍼스·반 삭제 또는 비활성화가 데이터 무결성 정책을 지킨다.
- [ ] `AUSER-011 P1` Subject 추가·수정·삭제와 Profile 연계 표시가 정상이다.

### 9.2 문제·분류·Guide

- [ ] `APROBLEM-001 P0` ProblemGroup을 유효한 영문 name·표시명으로 생성·수정한다.
- [ ] `APROBLEM-002 P1` 중복·잘못된 group name을 거부하고 참조 문제 수를 정확히 표시한다.
- [ ] `APROBLEM-003 P0` 문제 code, 이름, 그룹, 본문, 제한, 점수, 공개 상태, 언어로 문제를 생성한다.
- [ ] `APROBLEM-004 P0` code 형식·유일성, 필수 그룹, 시간·메모리·점수 범위를 검증한다.
- [ ] `APROBLEM-005 P0` 작성자·검수자·테스터를 지정하고 각 사용자의 편집·열람 권한이 반영된다.
- [ ] `APROBLEM-006 P0` 공개·비공개·대회 전용 전환이 사용자 목록·직접 URL·API가 아닌 UI에 반영된다.
- [ ] `APROBLEM-007 P1` 문제별 허용 언어와 언어별 시간·메모리 제한을 추가·수정·삭제한다.
- [ ] `APROBLEM-008 P1` 번역 이름·본문을 추가하고 사용자 언어에 맞춰 표시한다.
- [ ] `APROBLEM-009 P1` 공개 해설과 예약 해설을 추가·수정·삭제하고 공개 시점을 검증한다.
- [ ] `APROBLEM-010 P0` testcase zip 업로드·초기화 후 case 목록, batch, 점수, 입출력 파일을 확인한다.
- [ ] `APROBLEM-011 P0` 잘못된 zip, 누락 파일, 중복 case, 유효하지 않은 YAML을 거부하고 기존 데이터가 보존된다.
- [ ] `APROBLEM-012 P1` testcase 미리보기·파일 다운로드·diff가 권한 있는 사용자에게만 동작한다.
- [ ] `APROBLEM-013 P1` 문제를 새 code로 복제하고 원본 제출·승급전 이력과 분리된다.
- [ ] `APROBLEM-014 P1` 문제 내보내기가 권한 검사, 범위, 파일 내용과 인코딩을 지킨다.
- [ ] `APROBLEM-015 P1` 참조 제출·대회·승급 기록이 있는 문제 삭제가 보호 정책을 지킨다.
- [ ] `AGUIDE-001 P1` Guide를 유형, 제목, 요약, Markdown, 게시 상태, 순서로 생성한다.
- [ ] `AGUIDE-002 P1` created_by가 현재 관리자로 자동 기록되고 조작할 수 없다.
- [ ] `AGUIDE-003 P1` Guide 게시·미게시 전환과 순서 변경이 사용자 화면에 반영된다.
- [ ] `AGUIDE-004 P1` Markdown 미리보기에서 코드·수식·이미지가 실제 상세와 일치한다.
- [ ] `AGUIDE-005 P1` Guide 검색이 제목·요약·본문·유형 표시명으로 동작한다.

### 9.3 게임화

- [ ] `AGAME-001 P0` 난이도 클러스터를 티어·문제 그룹·필요 풀이 수·가중치·순서로 생성한다.
- [ ] `AGAME-002 P0` 같은 문제 그룹의 중복 클러스터와 0 이하 풀이 수·가중치를 거부한다.
- [ ] `AGAME-003 P1` 클러스터 활성·비활성 및 가중치 변경 후 사용자 점수 재계산 결과를 확인한다.
- [ ] `AGAME-004 P0` 출발 티어별 활성 승급전을 하나만 저장할 수 있다.
- [ ] `AGAME-005 P0` 승급전에 문제와 순서를 지정하고 사용자 해금 스냅샷이 정확히 생성된다.
- [ ] `AGAME-006 P0` 승급전 문제는 일반 문제 목록에서 숨겨지고 해금 사용자에게만 보인다.
- [ ] `AGAME-007 P1` 진행 중 시도와 완료 시도의 Profile·출발·목표 티어·시각은 읽기 전용이다.
- [ ] `AGAME-008 P1` 사용자 티어·점수·티어별 풀이 수 읽기 전용 정책이 지켜진다.
- [ ] `AGAME-009 P0` 승급 기록이 참조하는 시험·문제 삭제 시 보호 정책이 적용된다.

### 9.4 대회·참가·제출

- [ ] `ACONTEST-001 P0` key, 이름, 시작·종료, 공개 상태와 문제를 지정해 대회를 생성한다.
- [ ] `ACONTEST-002 P0` 시작이 종료보다 늦거나 같은 값, 잘못된 late deadline, 중복 key를 거부한다.
- [ ] `ACONTEST-003 P0` 비공개, 접근 코드, 허용 사용자, 허용 반, 차단 사용자를 각각 설정한다.
- [ ] `ACONTEST-004 P0` 대회 문제의 순서·라벨·점수·부분 점수·출제자를 저장하고 사용자 화면과 일치한다.
- [ ] `ACONTEST-005 P1` scoreboard 공개 정책, 시간 제한, rate, rating, freeze 관련 설정을 각 경계 시각에서 검증한다.
- [ ] `ACONTEST-006 P1` 대회 편집 권한자는 자기 대회만, 전체 편집 권한자는 모든 대회를 수정한다.
- [ ] `ACONTEST-007 P1` 잠긴 대회와 공개 상태를 권한 없는 스태프가 변경할 수 없다.
- [ ] `ACONTEST-008 P0` participation 추가·가상/실시간 시작·실격 상태 변경이 사용자 화면과 랭킹에 반영된다.
- [ ] `ACONTEST-009 P1` 같은 사용자·대회의 잘못된 중복 participation을 방지한다.
- [ ] `ASUBMIT-001 P0` 제출 Admin에서 사용자·문제·결과·언어·대회 필터와 검색이 정확하다.
- [ ] `ASUBMIT-002 P0` 단일·선택·문제 전체 재채점 권한과 확인 절차가 적용된다.
- [ ] `ASUBMIT-003 P0` 재채점 결과가 제출 상세, 문제 통계, 게임화, 대회 랭킹에 일관되게 반영된다.
- [ ] `ASUBMIT-004 P1` 제출 잠금·소스 공개·점수 수정 권한을 조작 POST로 우회할 수 없다.
- [ ] `ASUBMIT-005 P1` testcase 결과 inline은 허용 필드만 수정되고 합계가 일관된다.
- [ ] `ASUBMIT-006 P1` 대량 삭제·사용자 삭제 후 랭킹과 게임화 재계산이 고아 데이터를 남기지 않는다.

### 9.5 Judge·언어·기타 등록 모델

- [ ] `ARUNTIME-001 P0` Judge 추가·수정 후 bridge 연결 상태, 이름, key, 온라인 상태가 정확하다.
- [ ] `ARUNTIME-002 P0` 잘못된 Judge key·중복 이름·연결 끊김을 안전하게 처리한다.
- [ ] `ARUNTIME-003 P0` Language key, 표시명, 확장자, 템플릿, 실행 정보를 생성·수정한다.
- [ ] `ARUNTIME-004 P0` Judge가 지원하지 않는 언어는 제출 선택지에 나타나지 않는다.
- [ ] `ARUNTIME-005 P1` 언어 템플릿 변경이 제출 편집기의 초기 코드에 반영된다.
- [ ] `ARUNTIME-006 P1` 잘못된 과거 Language key와 정상 key가 혼동되지 않는다.
- [ ] `AOTHER-001 P1` NavigationBar 항목의 label, path, order, 부모 관계를 저장하고 노출 조건을 확인한다.
- [ ] `AOTHER-002 P1` 순환 부모·중복 또는 잘못된 경로를 안전하게 처리한다.
- [ ] `AOTHER-003 P1` License key·링크·표시명·아이콘·본문 CRUD가 사용자 상세에 반영된다.
- [ ] `AOTHER-004 P1` LogEntry는 읽기 전용이며 허용 권한자만 조회·삭제한다.
- [ ] `AOTHER-005 P1` PatchNote 모델이 Admin에 노출되는 설정이라면 CRUD·게시 표시 경로를 검증하고, 미노출이면 직접 URL을 차단한다.

## 10. 보안·개인정보·복원력

- [ ] `SEC-001 P0` 모든 상태 변경 POST가 CSRF 토큰 없이 거부된다. 명시적으로 예외인 가입 검증 엔드포인트는 입력만 검증하고 상태를 바꾸지 않는다.
- [ ] `SEC-002 P0` GUEST·STUDENT·AUTHOR·MANAGER별 보호 URL 직접 접근 결과가 역할 매트릭스와 일치한다.
- [ ] `SEC-003 P0` 객체 ID·username·problem code·contest key를 바꿔 다른 사용자의 비공개 자료에 접근할 수 없다.
- [ ] `SEC-004 P0` 소스 코드, testcase, 암호화 문제, 대회 접근 코드, API 토큰이 HTML·로그·오류 메시지에 노출되지 않는다.
- [ ] `SEC-005 P0` 회원가입, 로그인, 프로필, Guide·문제·대회 Markdown에 저장형·반사형 XSS가 실행되지 않는다.
- [ ] `SEC-006 P0` 검색·정렬·필터·Admin 입력에서 SQL 오류나 원문 query가 노출되지 않는다.
- [ ] `SEC-007 P0` 업로드 파일명 path traversal, 실행 파일, 허용 크기 초과, 잘못된 MIME을 거부한다.
- [ ] `SEC-008 P0` 다운로드 URL은 권한과 대상 소유자를 재검사하고 경로를 노출하지 않는다.
- [ ] `SEC-009 P1` HTTPS 강제, secure/session/CSRF cookie 속성, SameSite 정책이 운영 설정과 일치한다.
- [ ] `SEC-010 P1` 로그인·메일 재전송·비밀번호 재설정·제출의 반복 요청이 잠금 또는 제한 정책을 따른다.
- [ ] `SEC-011 P1` 사용자명·이메일 존재 여부가 인증·찾기 오류 차이로 과도하게 노출되지 않는다.
- [ ] `SEC-012 P1` 계정 비활성·삭제·권한 회수 후 기존 세션과 직접 URL 접근이 차단된다.
- [ ] `SEC-013 P1` 동시 제출·동시 재채점·동시 승급 완료가 중복 점수·중복 시도·데이터 경합을 만들지 않는다.
- [ ] `SEC-014 P1` DB·Redis·Judge의 일시 연결 실패 후 재시도·오류 화면·복구가 데이터 중복 없이 동작한다.
- [ ] `SEC-015 P2` 브라우저 콘솔에 비밀값, 심각한 JS 오류, 반복 네트워크 오류가 없다.

## 11. 접근성·반응형·호환성

- [ ] `UI-001 P1` 키보드만으로 로그인, 문제 검색, 문제 선택, 제출, 로그아웃 핵심 흐름을 완료한다.
- [ ] `UI-002 P1` 포커스 표시와 탭 순서가 시각적 순서와 일치하고 모달·메뉴에서 포커스가 유실되지 않는다.
- [ ] `UI-003 P1` 폼 label, 필수값, 오류 메시지가 스크린리더로 연관되어 읽힌다.
- [ ] `UI-004 P1` 링크와 버튼이 의미 있는 접근성 이름을 가지며 색상만으로 상태를 전달하지 않는다.
- [ ] `UI-005 P1` 라이트·다크 테마에서 본문, 링크, 상태 배지, 코드 블록의 명암 대비가 충분하다.
- [ ] `UI-006 P1` 200% 확대에서도 메뉴·폼·표·버튼이 겹치거나 잘리지 않는다.
- [ ] `UI-007 P1` 320px·375px·768px·1024px·1440px 폭에서 핵심 화면이 사용 가능하다.
- [ ] `UI-008 P1` 가로·세로 회전과 모바일 주소창 크기 변화에서도 고정 요소가 콘텐츠를 가리지 않는다.
- [ ] `UI-009 P2` Chrome·Firefox·Safari 계열에서 로그인, Select2, Ace 편집기, Markdown, MathJax가 동작한다.
- [ ] `UI-010 P2` 느린 네트워크에서 로딩·중복 클릭 방지·오류 재시도가 명확하다.
- [ ] `UI-011 P2` 이미지·폰트·외부 CDN 일부 실패에도 핵심 탐색과 텍스트가 사용 가능하다.
- [ ] `UI-012 P2` 한국어·영문, 긴 사용자명·문제명·대회명에서 줄바꿈과 표 레이아웃이 유지된다.

## 12. 최종 사용자 여정

- [ ] `JOURNEY-001 P0` 신규 사용자: 가입 → 이메일 인증 → 로그인 → Guide 학습 → 문제 선택 → PY3 제출 → AC → 홈·프로필·랭킹 반영을 완료한다.
- [ ] `JOURNEY-002 P0` 다중 언어 학습자: 같은 문제를 C·CPP17·JAVA(Java 17)로 제출하고 결과와 언어별 제한을 확인한다.
- [ ] `JOURNEY-003 P0` 승급 사용자: 요구 문제 풀이 → 승급전 해금 → 모든 문제 AC → 다음 티어 승급 → 랭킹 재정렬을 확인한다.
- [ ] `JOURNEY-004 P0` 대회 참가자: 허용 반 로그인 → 접근 코드 입력 → 참가 → 문제 제출 → 랭킹 확인 → 종료 후 결과 확인을 완료한다.
- [ ] `JOURNEY-005 P0` 문제 운영자: Admin 문제 생성 → testcase 업로드 → 공개 → 일반 계정 제출 → 재채점 → 결과 반영을 완료한다.
- [ ] `JOURNEY-006 P0` 대회 운영자: Admin 대회·문제·허용 반 구성 → 참가자 참여 → 제출 → 실격·재채점 → 결과 다운로드를 완료한다.
- [ ] `JOURNEY-007 P1` 콘텐츠 운영자: Guide 작성·미리보기·게시 → 사용자 열람 → 수정·미게시 전환을 완료한다.
- [ ] `JOURNEY-008 P1` 계정 복구: 로그인 실패 잠금 → 잠금 해제 → 비밀번호 재설정 → 2FA 설정 → 재로그인을 완료한다.
- [ ] `JOURNEY-009 P1` 권한 회수: AUTHOR 또는 MANAGER 권한 제거 후 메뉴·Admin·직접 URL·기존 세션 접근이 모두 차단된다.
- [ ] `JOURNEY-010 P2` 장애 복구: Judge 일시 중단 중 제출 → Judge 복구 → 채점 완료 → 중복 점수 없음까지 확인한다.

## 13. 실행 기록 양식

각 체크 항목을 실행할 때 다음 정보를 함께 기록한다.

- 실행 일시와 대상 환경·배포 커밋
- 테스트 ID와 실행자
- 사용자 역할과 사용한 테스트 데이터 식별자
- 브라우저·OS·viewport
- 실제 결과와 기대 결과
- 성공·실패·차단 상태
- 실패 시 screenshot, video, console, network, 서버 로그 위치
- 생성·수정한 데이터와 원복 여부
- 결함 ID와 재검증 결과
